class Circulo():
    def __init__(self, pontox, pontoy, raio):
        self.pontox = pontox
        self.pontoy = pontoy
        self.raio = raio
        
from Circulo import Circulo
circ1 = Circulo(1,1,10)
circ1.total_circulos1
circ2 = Circulo(2,2,20)
circ2.total_circulos2
Circulo.total_circulos
